# 2. Props and State

## Concept

- **Props**: Data passed from parent to child component. Read-only.
- **State**: Local data managed by the component. Can change over time.


##  Concept Overview

### 🔹 Props
- **Definition**: Data passed **from parent to child** component.
- **Behavior**: **Immutable** inside child components.
- **Use Case**: Sharing information between components.

### 🔸 State
- **Definition**: Local data **owned and managed** by the component.
- **Behavior**: **Mutable** via React hooks (e.g., `useState`).
- **Use Case**: Tracking dynamic, interactive behavior (UI updates).

---

##  Intuition

- **Props** are like ingredients from a supplier – the chef (component) can't change them.
- **State** is the chef’s own spice box – can be changed anytime during cooking.

---

## Example

```jsx
import { useState } from 'react';

function Counter({ step }) {
  const [count, setCount] = useState(0);
  return (
    <div>
      <p>Count: {count}</p>
      <button onClick={() => setCount(count + step)}>Increment</button>
    </div>
  );
}

// Usage
<Counter step={2} />
```

## Key Points

- Props are immutable in the child.
- State is managed using `useState` in functional components.
- Changing state triggers a re-render.

## Interview Questions & Answers

### Q1: What's the difference between Props and State?

**Answer:**

| Feature | Props | State |
|---------|-------|-------|
| **Source** | Passed from parent | Managed by component |
| **Mutability** | Read-only | Can be changed |
| **Trigger Re-render** | ✅ Yes (when changed in parent) | ✅ Yes (when updated via setState) |
| **Lifetime** | During component existence | During component existence |
| **Function** | Configuration/Data | UI interaction/Dynamic data |

```jsx
// Props: Read-only data from parent
function GreetUser({ name }) {  // 'name' is a prop
  return <h1>Hello, {name}!</h1>;
}

// State: Component's own data
function Counter() {
  const [count, setCount] = useState(0);  // 'count' is state
  return <button onClick={() => setCount(count + 1)}>Count: {count}</button>;
}
```

---

### Q2: Can you modify props in a child component?

**Answer:** **NO**, props are read-only. If you try to modify them, you'll get an error or unexpected behavior.

```jsx
// ❌ WRONG: Trying to modify props
function BadComponent({ user }) {
  user.name = 'NewName';  // This violates React principles
  return <p>{user.name}</p>;
}

// ✅ CORRECT: Use state if you need to modify data
function GoodComponent({ initialName }) {
  const [name, setName] = useState(initialName);
  
  const handleChangeName = () => {
    setName('NewName');
  };

  return (
    <div>
      <p>{name}</p>
      <button onClick={handleChangeName}>Change Name</button>
    </div>
  );
}
```

**Interview Follow-up:** "What if a child needs to update parent data?"
- **Answer:** Use a callback function passed as a prop (prop drilling) or Context API.

---

### Q3: Interview Scenario - Parent-Child Communication with Props and State

```jsx
// Parent Component
function UserProfile() {
  const [userData, setUserData] = useState({
    name: 'Alice',
    age: 25,
    email: 'alice@example.com'
  });

  const [isEditing, setIsEditing] = useState(false);

  // Function to update parent state from child
  const handleUserUpdate = (updatedData) => {
    setUserData(updatedData);
    setIsEditing(false);
  };

  return (
    <div style={{ border: '2px solid blue', padding: '20px' }}>
      <h2>User Profile (Parent)</h2>
      
      {isEditing ? (
        <UserEditor
          user={userData}
          onSave={handleUserUpdate}
          onCancel={() => setIsEditing(false)}
        />
      ) : (
        <UserDisplay
          user={userData}
          onEdit={() => setIsEditing(true)}
        />
      )}
    </div>
  );
}

// Child Component 1: Display User
function UserDisplay({ user, onEdit }) {
  return (
    <div>
      <p><strong>Name:</strong> {user.name}</p>
      <p><strong>Age:</strong> {user.age}</p>
      <p><strong>Email:</strong> {user.email}</p>
      <button onClick={onEdit}>Edit</button>
    </div>
  );
}

// Child Component 2: Edit User
function UserEditor({ user, onSave, onCancel }) {
  const [formData, setFormData] = useState(user);

  const handleChange = (e) => {
    const { name, value } = e.target;
    setFormData(prev => ({
      ...prev,
      [name]: value
    }));
  };

  return (
    <div>
      <input
        type="text"
        name="name"
        value={formData.name}
        onChange={handleChange}
        placeholder="Name"
      />
      <input
        type="number"
        name="age"
        value={formData.age}
        onChange={handleChange}
        placeholder="Age"
      />
      <input
        type="email"
        name="email"
        value={formData.email}
        onChange={handleChange}
        placeholder="Email"
      />
      <button onClick={() => onSave(formData)}>Save</button>
      <button onClick={onCancel}>Cancel</button>
    </div>
  );
}
```

**Key Concepts:**
- Parent has state (`userData`)
- Parent passes data as props to children
- Children pass callback functions as props to communicate back to parent

---

### Q4: Multiple State Values Management

```jsx
function ProductForm() {
  // Option 1: Multiple useState (Simple) ✅
  const [name, setName] = useState('');
  const [price, setPrice] = useState('');
  const [category, setCategory] = useState('');

  // Option 2: Single useState with object (Better for related data) ✅
  const [product, setProduct] = useState({
    name: '',
    price: '',
    category: ''
  });

  // Option 3: useReducer (Best for complex state) ✅
  const [state, dispatch] = useReducer(productReducer, initialState);

  // Handle multiple inputs with single handler
  const handleProductChange = (e) => {
    const { name, value } = e.target;
    setProduct(prev => ({
      ...prev,
      [name]: value
    }));
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    console.log('Product:', product);
  };

  return (
    <form onSubmit={handleSubmit}>
      <input
        type="text"
        name="name"
        value={product.name}
        onChange={handleProductChange}
        placeholder="Product Name"
      />
      <input
        type="number"
        name="price"
        value={product.price}
        onChange={handleProductChange}
        placeholder="Price"
      />
      <select name="category" value={product.category} onChange={handleProductChange}>
        <option value="">Select Category</option>
        <option value="Electronics">Electronics</option>
        <option value="Clothing">Clothing</option>
      </select>
      <button type="submit">Submit</button>
    </form>
  );
}

function productReducer(state, action) {
  switch (action.type) {
    case 'UPDATE_FIELD':
      return { ...state, [action.field]: action.value };
    case 'RESET':
      return initialState;
    default:
      return state;
  }
}

const initialState = { name: '', price: '', category: '' };
```

---

### Q5: State Updates and Re-renders

```jsx
function StateUpdateDemo() {
  const [count, setCount] = useState(0);
  const [message, setMessage] = useState('');

  console.log('Component rendered');

  // ❌ WRONG: State update doesn't happen immediately
  const handleWrongClick = () => {
    setCount(count + 1);
    console.log(count);  // Still shows old value!
  };

  // ✅ CORRECT: Use callback to see updated state
  const handleCorrectClick = () => {
    setCount(prev => prev + 1);
    // State update will trigger re-render
  };

  // ✅ CORRECT: Use effect to see state changes
  useEffect(() => {
    console.log('Count changed to:', count);
  }, [count]);

  return (
    <div>
      <p>Count: {count}</p>
      <button onClick={handleWrongClick}>Click (Wrong - check console)</button>
      <button onClick={handleCorrectClick}>Click (Correct)</button>
    </div>
  );
}
```

**Key Point:** State updates are **batched** in React. You don't see the update immediately; you need to wait for the next render or use `useEffect`.

---

### Q6: Props Drilling Problem & Solution

```jsx
// Problem: Props drilling through multiple levels
function GrandParent() {
  const [theme, setTheme] = useState('light');

  return (
    <Parent theme={theme} />
  );
}

function Parent({ theme }) {
  // Doesn't use theme but has to pass it
  return <Child theme={theme} />;
}

function Child({ theme }) {
  // Doesn't use theme but has to pass it
  return <GrandChild theme={theme} />;
}

function GrandChild({ theme }) {
  // Finally uses theme
  return <p>Theme: {theme}</p>;
}

// Solution: Use Context API
const ThemeContext = createContext();

function GrandParentWithContext() {
  const [theme, setTheme] = useState('light');

  return (
    <ThemeContext.Provider value={{ theme, setTheme }}>
      <Parent />
    </ThemeContext.Provider>
  );
}

function Parent() {
  return <Child />;  // No need to pass theme
}

function Child() {
  return <GrandChild />;  // No need to pass theme
}

function GrandChild() {
  const { theme } = useContext(ThemeContext);
  return <p>Theme: {theme}</p>;
}
```

---

### Q7: Default Props

```jsx
// Function Component - Using default parameters
function Greeting({ name = 'Guest', age = 18 }) {
  return <p>Hello {name}, you are {age} years old</p>;
}

// Alternative: Using defaultProps
Greeting.defaultProps = {
  name: 'Guest',
  age: 18
};

// Usage
<Greeting />                           {/* Shows: Hello Guest, you are 18 */}
<Greeting name="Alice" />              {/* Shows: Hello Alice, you are 18 */}
<Greeting name="Bob" age={25} />      {/* Shows: Hello Bob, you are 25 */}
```

---

### Q8: Real-World Interview Scenario - Todo App with Props & State

```jsx
function TodoApp() {
  const [todos, setTodos] = useState([]);
  const [filter, setFilter] = useState('all');  // all, active, completed
  const [inputValue, setInputValue] = useState('');

  const addTodo = () => {
    if (inputValue.trim()) {
      setTodos([
        ...todos,
        { id: Date.now(), text: inputValue, completed: false }
      ]);
      setInputValue('');
    }
  };

  const toggleTodo = (id) => {
    setTodos(todos.map(todo =>
      todo.id === id ? { ...todo, completed: !todo.completed } : todo
    ));
  };

  const deleteTodo = (id) => {
    setTodos(todos.filter(todo => todo.id !== id));
  };

  const getFilteredTodos = () => {
    switch (filter) {
      case 'active':
        return todos.filter(todo => !todo.completed);
      case 'completed':
        return todos.filter(todo => todo.completed);
      default:
        return todos;
    }
  };

  const filteredTodos = getFilteredTodos();

  return (
    <div style={{ maxWidth: '500px', margin: '0 auto' }}>
      <h1>Todo App</h1>

      <div>
        <input
          type="text"
          value={inputValue}
          onChange={(e) => setInputValue(e.target.value)}
          onKeyPress={(e) => e.key === 'Enter' && addTodo()}
          placeholder="Add a new todo..."
        />
        <button onClick={addTodo}>Add</button>
      </div>

      <div>
        {['all', 'active', 'completed'].map(filterType => (
          <button
            key={filterType}
            onClick={() => setFilter(filterType)}
            style={{
              backgroundColor: filter === filterType ? '#007bff' : '#e9ecef',
              color: filter === filterType ? '#fff' : '#000'
            }}
          >
            {filterType.charAt(0).toUpperCase() + filterType.slice(1)}
          </button>
        ))}
      </div>

      <TodoList
        todos={filteredTodos}
        onToggle={toggleTodo}
        onDelete={deleteTodo}
      />

      <p>Total: {todos.length} | Completed: {todos.filter(t => t.completed).length}</p>
    </div>
  );
}

// Child Component receiving props and callbacks
function TodoList({ todos, onToggle, onDelete }) {
  return (
    <ul style={{ listStyle: 'none', padding: 0 }}>
      {todos.map(todo => (
        <TodoItem
          key={todo.id}
          todo={todo}
          onToggle={() => onToggle(todo.id)}
          onDelete={() => onDelete(todo.id)}
        />
      ))}
    </ul>
  );
}

function TodoItem({ todo, onToggle, onDelete }) {
  return (
    <li style={{
      padding: '10px',
      border: '1px solid #ddd',
      marginBottom: '5px',
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'center'
    }}>
      <label>
        <input
          type="checkbox"
          checked={todo.completed}
          onChange={onToggle}
        />
        <span style={{
          marginLeft: '10px',
          textDecoration: todo.completed ? 'line-through' : 'none'
        }}>
          {todo.text}
        </span>
      </label>
      <button onClick={onDelete}>Delete</button>
    </li>
  );
}
```

---

### Q9: State Immutability - Important for Interviews!

```jsx
// ❌ WRONG: Mutating state directly
function BadExample() {
  const [user, setUser] = useState({ name: 'Alice', age: 25 });

  const updateAge = () => {
    user.age = 26;  // Direct mutation (BAD!)
    setUser(user);  // React won't detect change
  };

  return <p>Age: {user.age}</p>;
}

// ✅ CORRECT: Creating new object
function GoodExample() {
  const [user, setUser] = useState({ name: 'Alice', age: 25 });

  const updateAge = () => {
    // Create new object (shallow copy)
    setUser({ ...user, age: 26 });
  };

  return <p>Age: {user.age}</p>;
}

// ✅ CORRECT: With nested objects
function NestedStateExample() {
  const [user, setUser] = useState({
    name: 'Alice',
    address: { city: 'NYC', zip: '10001' }
  });

  const updateCity = () => {
    setUser({
      ...user,
      address: { ...user.address, city: 'LA' }
    });
  };

  return <p>City: {user.address.city}</p>;
}
```

---

### Q10: PropTypes and Type Checking

```jsx
import PropTypes from 'prop-types';

function UserCard({ name, age, email, tags, onDelete }) {
  return (
    <div>
      <h3>{name}</h3>
      <p>Age: {age}</p>
      <p>Email: {email}</p>
      <p>Tags: {tags.join(', ')}</p>
      <button onClick={onDelete}>Delete</button>
    </div>
  );
}

// Define PropTypes
UserCard.propTypes = {
  name: PropTypes.string.isRequired,
  age: PropTypes.number,
  email: PropTypes.string.isRequired,
  tags: PropTypes.arrayOf(PropTypes.string),
  onDelete: PropTypes.func.isRequired
};

// Define default props
UserCard.defaultProps = {
  age: 18,
  tags: []
};

// Usage
<UserCard
  name="Alice"
  age={25}
  email="alice@example.com"
  tags={['developer', 'react']}
  onDelete={() => alert('Deleted!')}
/>
```

---

## Common Interview Mistakes

```jsx
// ❌ MISTAKE 1: Directly mutating state
const [items, setItems] = useState([1, 2, 3]);
items.push(4);  // Wrong!
setItems(items);

// ✅ CORRECT
setItems([...items, 4]);

// ❌ MISTAKE 2: Using setState in the same render
function BadComponent() {
  const [count, setCount] = useState(0);
  setCount(count + 1);  // Infinite loop!
  return <p>{count}</p>;
}

// ✅ CORRECT: Use useEffect
function GoodComponent() {
  const [count, setCount] = useState(0);
  
  useEffect(() => {
    // This runs after render
    setCount(prev => prev + 1);
  }, []);
  
  return <p>{count}</p>;
}

// ❌ MISTAKE 3: Modifying props directly
function BadProps({ items }) {
  items[0] = 'modified';  // Wrong!
  return <p>{items[0]}</p>;
}

// ✅ CORRECT: Create new state if needed
function GoodProps({ items }) {
  const [localItems, setLocalItems] = useState(items);
  
  const updateFirst = () => {
    setLocalItems([
      'modified',
      ...localItems.slice(1)
    ]);
  };
  
  return <div><p>{localItems[0]}</p></div>;
}
```

---

## Quick Reference: Props vs State

| Aspect | Props | State |
|--------|-------|-------|
| Who sets it? | Parent component | Component itself |
| Can child modify? | ❌ No | ✅ Yes |
| Re-render when changed? | ✅ Yes | ✅ Yes |
| Immutable? | ✅ Yes | ❌ No (but practice immutability) |
| Passed down? | ✅ Yes | ❌ No (unless via Context) |
| Good for sharing between components? | ✅ Yes | ❌ No (use lifting state up) |

---

## Practice Exercise

Build a **Counter with Reset** component:
1. Accept `initialValue` as prop (default 0)
2. Manage current count in state
3. Buttons to increment, decrement, and reset
4. Reset should use the initial prop value
5. Display counter value
6. Add a child component that displays if counter is "even" or "odd"
7. Pass callback to child to update parent

This covers props passing, state management, parent-child communication, and state updates!